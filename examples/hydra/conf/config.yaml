---
defaults:
  - scenario: simulation
  - _self_

# Example Hydra base config for a consuming repository's test suite.
# Compose with overlays the standard Hydra way, e.g.:
#   python -m pytest --hydra-overrides scenario=hardware
# or, inside a test, via the Compose API:
#   with initialize(version_base=None, config_path="conf"):
#       cfg = compose(config_name="config", overrides=["scenario=hardware"])
run_id: local
